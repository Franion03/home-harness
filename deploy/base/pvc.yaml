apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: harness-data
  namespace: assistant
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
